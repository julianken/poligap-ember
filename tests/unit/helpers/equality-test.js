
import { equality } from 'poligap-ember/helpers/equality';
import { module, test } from 'qunit';

module('Unit | Helper | equality');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = equality([42]);
  assert.ok(result);
});

