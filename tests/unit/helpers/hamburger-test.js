
import { hamburger } from 'poligap-ember/helpers/hamburger';
import { module, test } from 'qunit';

module('Unit | Helper | hamburger');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = hamburger([42]);
  assert.ok(result);
});

