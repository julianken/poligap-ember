
import { poligapBigLogo } from 'poligap-ember/helpers/poligap-big-logo';
import { module, test } from 'qunit';

module('Unit | Helper | poligap big logo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = poligapBigLogo([42]);
  assert.ok(result);
});

