
import { poligapLogo } from 'poligap-ember/helpers/poligap-logo';
import { module, test } from 'qunit';

module('Unit | Helper | poligap logo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = poligapLogo([42]);
  assert.ok(result);
});

