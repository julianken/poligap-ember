
import { clear } from 'poligap-ember/helpers/clear';
import { module, test } from 'qunit';

module('Unit | Helper | clear');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = clear([42]);
  assert.ok(result);
});

