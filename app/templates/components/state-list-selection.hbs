{{yield}}

<div class="map-container" style="position: relative">
  <h2 class="center-align state-list-title">Select a state to browse representative&nbsp;data.</h2>
  {{#if filteredList}}
  <ul class="states-list">
    {{#each filteredList as |state|}}
      {{#link-to 'state.show' state.id}}
        <li>{{state.data.fullName}}</li>
      {{/link-to}}
    {{/each}}
  </ul>
  {{else}}
    <p>No results</p>
  {{/if}}
</div>
